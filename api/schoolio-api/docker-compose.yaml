version: '3.8'

services:

  redis:
    image: redis:alpine
    ports:
      - "6379:6379"

  spring-boot:
    build:
      context: .
      args:

    ports:
      - "8080:8080"
    links:
      - redis
    depends_on:
      - redis

